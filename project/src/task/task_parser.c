#include "../../include/parser.h"


int parser_number(const char* number) {
    return -1;
}

int parser_description(const char** description) {
    return -1;
}

int parser_number(const char* priority) {

    return -1;
}

int parser_number(const char* data) {
    return -1;
}